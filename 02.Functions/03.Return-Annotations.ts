// regular function

function doubleNum(x: number): number {
  return x * x;
}

console.log(doubleNum(5));

//arrow function

const productNum = (x: number): number => x * x;

console.log(productNum(7));
