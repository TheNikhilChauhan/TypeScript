//Here we are inferring the types
// type inference is a feature that allows the compiler to automatically determine the type of a variable based on its value.
// if you declare a variable without explicitly specifying its type, TS will try to infer the type based on the value you assign to it.

let pl = "TypeScript";
let favNum = 3;
let isTrue = true;

console.log(pl, favNum, isTrue);
